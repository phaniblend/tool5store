# Local dev only — Railway/Cloud Run set PORT themselves at deploy time.
PORT=8080

# Comma-separated browser origins allowed to call this API via CORS.
ALLOWED_ORIGINS=https://tool5.store,https://app.tool5.store

# Set to log per-chapter conversion progress.
DEBUG_CONVERT=
