# Local dev only — Cloud Run sets PORT itself at deploy time.
PORT=8080

# Comma-separated browser origins allowed to call this API via CORS.
# Leave empty for server-to-server-only use (no browser origin allowed).
ALLOWED_ORIGINS=https://tool5.store,https://app.tool5.store
